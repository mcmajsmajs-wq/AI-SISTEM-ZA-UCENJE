# ================================================================================
# LOKALNI DEV/TEST DEPENDENCIES
# ================================================================================
# Minimalni paketi za pokretanje unit testova lokalno (bez Dockera).
# Svi paketi koji zahtevaju sistem libs (fitz, psycopg2, redis)
# su ovde sa lakim alternativama.
#
# Instalacija:
#   python3 -m venv .venv
#   source .venv/bin/activate
#   pip install -r requirements-dev.txt
# ================================================================================

# Test framework
pytest==7.4.3
pytest-asyncio==0.21.1
pytest-timeout==2.4.0
pytest-mock==3.12.0
pytest-cov==4.1.0

# Web framework (za import testove)
fastapi==0.104.1
uvicorn==0.24.0
python-multipart==0.0.6

# Data validation
pydantic==2.5.0
pydantic-settings==2.1.0
email-validator==2.1.0

# Database (SQLite za testove, bez psycopg2)
sqlalchemy==2.0.23
alembic==1.12.1

# Auth
passlib[bcrypt]==1.7.4
python-jose[cryptography]==3.3.0
PyJWT==2.8.0
cryptography==41.0.7

# HTTP
httpx==0.27.2
requests==2.31.0

# Utilities
tenacity==8.2.3
python-dotenv==1.0.0
python-dateutil==2.8.2

# Redis (mock-friendly)
redis==5.0.1
fakeredis==2.23.2

# Celery (za import)
celery==5.3.4

# PDF (lite — bez system libs)
reportlab==4.0.7
Pillow==10.1.0

# AI (lite — bez heavy models)
tiktoken==0.7.0
openai==1.35.0

# Async
aiofiles==23.2.1
anyio>=4.0.0
